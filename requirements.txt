# Core Django framework
Django>=4.2,<5.0

# PostgreSQL database adapter
psycopg2-binary>=2.9

# Gunicorn: production WSGI server (used by Render, not locally on Windows)
gunicorn

# Whitenoise: serve static files efficiently
whitenoise>=6.5.0

# Pillow: image processing (needed if you use ImageField in models)
Pillow

# Django Environ: for environment variables (.env handling)
django-environ
